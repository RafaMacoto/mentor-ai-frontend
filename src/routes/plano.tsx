import { createFileRoute } from "@tanstack/react-router"; import { Check, Circle, Clock3, LockKeyhole, Route as RouteIcon } from "lucide-react"; import { AppShell } from "@/components/mentor/AppShell"; import { Button } from "@/components/ui/button";
export const Route=createFileRoute("/plano")({component:Plano});
const phases=[{title:"Fundamentos",status:"Concluído",tasks:["Revisar fundamentos de Java","Revisar orientação a objetos","Praticar Collections"]},{title:"Backend",status:"Em andamento",tasks:["Criar API REST","Trabalhar com Spring Boot","Implementar autenticação","Criar testes"]},{title:"Cloud e DevOps",status:"Próxima fase",tasks:["Docker","CI/CD","Cloud"]}];
function Plano(){return <AppShell><div><p className="text-sm font-semibold text-primary">Sua trilha</p><h1 className="mt-1 text-3xl font-semibold">Seu plano de desenvolvimento</h1><p className="mt-2 max-w-2xl text-muted-foreground">Uma sequência personalizada de estudos e prática para aproximar você do seu objetivo.</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="surface-card p-5"><p className="text-xs text-muted-foreground">Progresso geral</p><p className="mt-2 text-2xl font-semibold">42%</p></div><div className="surface-card p-5"><p className="text-xs text-muted-foreground">Tarefas concluídas</p><p className="mt-2 text-2xl font-semibold">12 / 28</p></div><div className="surface-card p-5"><p className="text-xs text-muted-foreground">Próxima fase</p><p className="mt-2 text-lg font-semibold">Backend</p></div></div><div className="mt-8 space-y-5">{phases.map((phase,i)=><section key={phase.title} className="surface-card overflow-hidden"><div className="flex items-center gap-4 border-b border-border p-5"><span className={`flex size-10 items-center justify-center rounded-full ${i===0?'bg-accent text-primary':i===1?'bg-primary text-primary-foreground':'bg-muted text-muted-foreground'}`}>{i===0?<Check/>:i===1?<RouteIcon/>:<LockKeyhole/>}</span><div className="flex-1"><h2 className="font-semibold">Fase {i+1} · {phase.title}</h2><p className="text-xs text-muted-foreground">{phase.status}</p></div>{i===1&&<span className="rounded-full bg-accent px-3 py-1 text-xs font-semibold text-accent-foreground">Atual</span>}</div><div className="divide-y divide-border">{phase.tasks.map((task,j)=><div key={task} className="flex items-center gap-4 p-4 sm:px-5"><span className="text-muted-foreground">{i===0?<Check className="size-5 text-primary"/>:i===1&&j===0?<Circle className="size-5 text-primary"/>:<LockKeyhole className="size-4"/>}</span><div className="flex-1"><p className={`text-sm font-medium ${i===0?'line-through text-muted-foreground':''}`}>{task}</p><p className="mt-0.5 flex items-center gap-1 text-xs text-muted-foreground"><Clock3 className="size-3"/> {i===0?'30 min':'1–2h'} estimados</p></div>{i===1&&j===0&&<Button size="sm">Começar</Button>}</div>)}</div></section>)}</div></div></AppShell>}
